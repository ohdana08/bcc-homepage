# BCC 홈페이지 통합 로그인 조사

> 조사 범위: GitHub Pages 정적 사이트에서 카카오·네이버·구글 로그인을 붙이는 현실적인 방법. 이번 단계에서는 구현하지 않는다.

## 먼저 확인할 결론

GitHub Pages만으로 HTML을 숨기는 것은 보안이 아니다. 로그인 후 보호해야 하는 수강 정보·결제 정보·개인정보는 반드시 인증 제공자의 토큰을 검증하는 백엔드 또는 행 단위 접근제어가 적용된 데이터 서비스 뒤에 둬야 한다.

현재 저장소에는 Supabase 사용 흔적이 있으므로, 새 인증 체계를 추가하기보다 **기존 회원 데이터와 Supabase Auth를 기준으로 통합 가능성을 먼저 검증하는 방법**이 비용과 중복 계정 위험이 가장 낮다.

## 방법 1 — Supabase Auth 중심 통합 (우선 검토)

정적 페이지는 `supabase-js`로 OAuth 리디렉션을 시작하고, 로그인 콜백과 세션은 Supabase Auth가 처리한다. 공식 문서상 Google과 Kakao 소셜 로그인을 지원한다. 네이버는 기본 제공자 목록에 없으므로 별도 OAuth 브리지 또는 커스텀 백엔드가 필요하다.

- 장점: 기존 Supabase 데이터와 사용자 ID·RLS를 한곳에서 관리하기 쉽다. Google·Kakao 적용 경로가 명확하다.
- 단점: 네이버만 별도 구현이 필요하다. 기존 이메일 회원과 소셜 계정의 병합 정책을 먼저 정해야 한다.
- 적합한 경우: 수강 이력과 회원 데이터를 Supabase에 계속 둘 계획일 때.
- 공식 자료: [Supabase Social Login](https://supabase.com/docs/guides/auth/social-login), [Kakao 설정](https://supabase.com/docs/guides/auth/social-login/auth-kakao), [Google 설정](https://supabase.com/docs/guides/auth/social-login/auth-google)

## 방법 2 — Firebase Authentication + 네이버/카카오 커스텀 토큰

Google은 Firebase 웹 SDK로 바로 연결하고, 카카오·네이버 로그인 결과를 서버리스 함수에서 검증한 뒤 Firebase Custom Token을 발급하는 구성이다.

- 장점: Google 로그인이 간단하고, 인증 SDK·세션 관리가 성숙했다. 여러 제공자를 하나의 Firebase UID로 연결할 수 있다.
- 단점: 카카오·네이버용 서버리스 검증 코드가 필요하다. 기존 Supabase 회원/수강 데이터와 Firebase UID를 매핑해야 하므로 운영 복잡도가 커진다.
- 적합한 경우: 앞으로 데이터와 서버리스 기능을 Firebase 중심으로 이전할 계획일 때.
- 공식 자료: [Firebase Google 로그인](https://firebase.google.com/docs/auth/web/google-signin), [계정 제공자 연결](https://firebase.google.com/docs/auth/web/account-linking)

## 방법 3 — 인증 브로커 또는 자체 서버리스 OAuth 게이트웨이

Auth0·WorkOS 같은 인증 브로커 또는 Vercel/Cloudflare Functions를 로그인 콜백 서버로 두고, Google·Kakao·Naver OAuth를 하나의 내부 사용자 ID로 정규화한다.

- 장점: 세 제공자를 동일한 로그인 UX와 계정 병합 정책으로 통제할 수 있다. 공급자 추가에 유연하다.
- 단점: 가장 많은 설계·보안·운영 책임이 따른다. OAuth state/PKCE, 토큰 저장, 탈퇴·연결 해제, 계정 충돌 처리가 필요하며 비용도 늘 수 있다.
- 적합한 경우: BCC 외 여러 서비스까지 하나의 계정 체계로 묶거나 복잡한 권한 체계가 필요할 때.
- 네이버 참고: [네이버 아이디로 로그인 공식 안내](https://developers.naver.com/products/login/api)

## 홈페이지에 로그인이 필요한 시나리오

1. 구매한 강의와 수강 링크를 본인에게만 제공할 때
2. 결제·환불·신청 이력을 개인별로 조회할 때
3. 잇툴즈 사용량, 저장한 분석 결과, 생성 이력을 보관할 때
4. 회원 전용 템플릿·자료 다운로드 권한을 제어할 때
5. 상담 신청 상태와 답변을 마이페이지에서 확인할 때

반대로 단순 회사 소개, 공개 강의 안내, 무료 도구 체험, 문의 접수에는 로그인을 강제하지 않는 편이 전환에 유리하다.

## 권장 의사결정 순서

1. 기존 회원의 기준 ID와 현재 Supabase 스키마를 확인한다.
2. Google·Kakao만 먼저 필요한지, 네이버까지 첫 출시 필수인지 결정한다.
3. 동일 이메일의 기존 계정과 소셜 계정을 어떻게 병합할지 정한다.
4. 로그인 후 보호할 데이터와 공개 데이터를 분리한다.
5. 개인정보처리방침, 탈퇴, 소셜 연결 해제 절차를 함께 설계한다.

현 상태에서는 **Supabase Auth + Google/Kakao를 1차 후보**로 두고, 네이버의 실제 이용자 수요가 확인되면 OAuth 브리지를 추가하는 단계적 접근을 권장한다.
