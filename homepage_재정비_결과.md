# BCC 홈페이지 재정비 결과

작업일: 2026-07-02  
대상: GitHub Pages 정적 홈페이지 `/Users/jinjoopwer/bcc-homepage`

## 지킨 범위

- `bcc-homepage` 폴더 안의 공개 홈페이지 파일만 수정했다.
- admin 관련 파일과 다른 프로젝트는 수정하지 않았다.
- 기존 콘텐츠를 삭제하지 않았다.
- 기존 CRM 문의 폼의 필드·엔드포인트·전송 로직은 변경하지 않았다.
- 결제·로그인 기능은 구현하지 않았다.
- 보호 대상 GA4 측정 ID `G-5TN5XW0T2J`를 유지했고, 새 지표 페이지에도 동일 ID를 설치했다.

## 작업 1 — 진입점 통합과 즉시 가입 동선

### 변경 내용

- 메인 첫 화면에 목적별 빠른 경로 3개를 추가했다.
  - 실전 강의
  - 무료 잇툴즈
  - 기업·기관 강의/컨설팅 문의
- 모바일 메인 하단에 `회원가입`과 `가입·문의` 고정 CTA를 추가했다.
- 숏폼·카드뉴스 강의 페이지의 주요 CTA를 기존 CRM 문의 폼으로 직접 연결했다.
- 잇툴즈 허브·GPTs·네이버 분석기에 회원가입 및 CRM 문의 진입점을 추가했다.
- 전환 CTA에 `utm_source`, `utm_medium`, `utm_campaign`, `utm_content`를 부여했다.
- 강의 페이지의 기존 유입 UTM 병합 코드가 해시 뒤에 값을 붙이지 않도록 보완했다.

### 변경 파일

- `index.html`
- `shorts.html`
- `cardnews.html`
- `tools/index.html`
- `tools/naver/index.html`
- `ittools-gpts.html`

### 확인 방법

1. 각 페이지를 데스크톱과 모바일 폭으로 연다.
2. 상단·본문·하단 CTA가 `index.html#contact` 또는 `signup.html`로 1클릭 이동하는지 확인한다.
3. CTA URL에 UTM 4개 항목이 포함되는지 확인한다.
4. 문의 폼 제출 전 필드와 기존 동작이 동일한지 확인한다. 실제 제출은 QA에서 수행하지 않았다.

## 작업 2 — 신뢰형 UI/UX 구조 개선

### 변경 전

`브랜드 소개 → 서비스 → 잇툴즈 → 잇스쿨 → 문의`

### 변경 후

`핵심 가치·신뢰 근거 → 방문 목적별 빠른 선택 → 기존 소개·서비스 → 잇툴즈·잇스쿨 → 단일 CRM 문의`

공공·교육기관형 사이트에서 자주 쓰는 대상별 빠른 선택, 서비스 분류, 단일 상담 창구 패턴을 BCC 규모에 맞게 단순화했다. 기존 섹션과 콘텐츠는 유지했다.

벤치마크 참고:

- [K-Startup 창업지원포털](https://www.k-startup.go.kr/web/main/index.do)
- [스타트업 원스톱 지원센터](https://www.k-startup.go.kr/onestop)

### 모바일 우선 적용

- 3열 빠른 선택 카드는 모바일에서 1열로 변경된다.
- 모바일 화면에 고정 전환 바를 제공한다.
- 고정 바 공간만큼 본문 하단 여백을 확보했다.
- 기존 플로팅 카카오 버튼과 모바일 고정 바가 겹치지 않도록 모바일에서 플로팅 버튼만 숨겼다.

### 확인 방법

- 390×844 화면에서 가로 넘침이 없는지 확인한다.
- 빠른 선택 카드가 1열인지, 모바일 고정 CTA가 화면 하단에 표시되는지 확인한다.

## 작업 3 — 지표 자리 마련

### 생성 파일

- `insights-preview-7f3a.html`

### 구성

- 월 매출
- API 비용
- 리드 수
- 전환율

모든 값은 더미 데이터다. `noindex,nofollow,noarchive`를 적용했으며 API 호출과 실데이터 연동 코드는 없다. 경로를 아는 사람은 접근할 수 있으므로 “비공개 경로”는 보안 접근제어가 아니라 검색 노출 방지 수준이다.

### 확인 방법

- 카드가 정확히 4개인지 확인한다.
- 화면의 더미 데이터 안내와 `noindex` 메타 태그를 확인한다.
- 네트워크/API 요청 코드가 없는지 확인한다.

## 작업 4 — 통합 로그인 조사

### 생성 파일

- `통합로그인_조사.md`

### 조사 결과

1. Supabase Auth 중심 통합 — Google·Kakao 우선, Naver는 별도 브리지
2. Firebase Authentication — Google 기본, Kakao·Naver는 커스텀 토큰
3. 인증 브로커/서버리스 OAuth 게이트웨이 — 유연하지만 운영 책임이 가장 큼

현재 구조에는 Supabase 사용 흔적이 있으므로, 기존 회원 ID와 데이터 구조를 확인한 뒤 Supabase Auth를 1차 후보로 검토하는 안을 권장했다. 구현 코드는 작성하지 않았다.

## 검증 결과

- 메인 빠른 경로: 3개
- 기존 CRM 문의 폼: 유지
- 메인 GA4 `G-5TN5XW0T2J`: 유지 확인
- 모바일 390×844: 가로 넘침 없음
- 모바일 전환 바: 고정 표시 확인
- 지표 카드: 4개
- 지표 페이지 GA4: `G-5TN5XW0T2J` 확인
- 지표 페이지 API 호출: 없음
- 지표 페이지 검색 제외 메타: 적용
- 실제 CRM 제출·결제·로그인: 수행하지 않음

## 배포 전 수동 확인 권장

1. GitHub Pages 미리보기에서 외부 잇툴즈 링크가 정상인지 확인한다.
2. 실제 휴대폰 Safari/Chrome에서 하단 고정 CTA가 브라우저 UI와 겹치지 않는지 확인한다.
3. CRM 운영 환경에서 테스트용 문의 1건을 제출하려면 개인정보·CRM 기록 생성이 발생하므로 운영자 승인 후 수행한다.

## 후속 개선 — 단일 페이지 연속 스크롤

- 상단 메뉴의 강의 후기, 회원가입, 로그인을 별도 페이지 링크에서 메인 내부 앵커로 변경했다.
- 강의 후기 게시판 전체를 메인 `#reviews` 섹션에 포함했다.
- 기존 회원가입과 로그인 기능을 각각 `#signup`, `#login` 섹션에 포함했다.
- 포함된 기존 페이지의 제출·인증 로직은 변경하지 않고 표시 방식만 조정했다.
- iframe 높이를 콘텐츠 변화에 맞춰 자동 조정해 내부 이중 스크롤을 줄였다.
- 모바일 390px 폭에서 1열 배치와 가로 넘침 없음이 확인됐다.
